{
  "name": "klsh",
  "version": "0.1.0",
  "description": "Minimal text-based pipeline framework for browser environment",
  "main": "dist/klsh.js",
  "scripts": {
    "test": "npm run build && mocha --recursive tests",
    "build": "node build.js",
    "parse": "node build.js && exec $(command -v rlwrap >/dev/null 2>&1 && echo rlwrap) node parse.js",
    "start": "node build.js && command -v rlwrap >/dev/null && rlwrap node klsh.js || node klsh.js"
  },
  "keywords": [
    "shell",
    "pipeline",
    "text"
  ],
  "author": "",
  "license": "Apache-2.0",
  "devDependencies": {
    "mocha": "11.5.0",
    "chai": "5.2.0",
    "jison": "^0.4.18",
    "fake-indexeddb": "^3.1.7"
  }
}
